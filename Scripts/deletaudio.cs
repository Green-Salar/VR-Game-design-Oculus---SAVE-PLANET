using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class deletaudio : MonoBehaviour
{
    public GameObject au;       
    // Start is called before the first frame update
    void Start()
    {
        au.SetActive(false);    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
