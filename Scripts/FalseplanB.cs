using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FalseplanB : MonoBehaviour
{
    public GameObject b;
    public GameObject a;


    // Start is called before the first frame update
    void Start()
    {
        b.SetActive(false);
        a.SetActive(false);


    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
